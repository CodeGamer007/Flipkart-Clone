function login() {
    let blur = document.getElementById('blur');
    blur.classList.toggle('active-blur');

    let form = document.getElementById('form');
    form.classList.toggle('active-blur');
}